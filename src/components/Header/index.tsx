import * as React from "react";
import styled from "styled-components";

export default function Header() {
  return <Wrapper>Gavin O'Brien</Wrapper>;
}

const Wrapper = styled.div`
  width: 100%;
  background-color: red;
`;
